version: "3.9"

services:
  bot:
    build: .
    ports:
      - "8005:8005"
    env_file:
      - .env
    environment:
      - WS_URI=${WS_URI:-ws://localhost:8005/ws}
    volumes:
      - ./private.key:/app/private.key:ro
